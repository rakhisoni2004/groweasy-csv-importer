export const BATCH_SIZE = 20;
export const MAX_RETRIES = 2;
export const GEMINI_MODEL = "gemini-2.0-flash";
export const MAX_PREVIEW_ROWS = 500; // safety cap so a huge CSV doesn't freeze the browser table
